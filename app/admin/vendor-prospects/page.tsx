import AdminVendorProspectsClient from '@/components/AdminVendorProspectsClient';

export const metadata = {
  title: 'Vendor Prospects — Admin',
  robots: { index: false, follow: false },
};

export default function AdminVendorProspectsPage() {
  return <AdminVendorProspectsClient />;
}
