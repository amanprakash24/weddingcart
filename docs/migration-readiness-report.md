# Migration Readiness Report — 2026-07-19

Generated by `scripts/validate-migration-readiness.mjs` against live production
MongoDB data, after resolving the issues found on the first run.

## Pre-fix run (found 1 blocking issue via manual audit, then 4 more via the validator)

- **`bridal-jewellery` category missing** — vendor "Shringaar Bridal Jewels" referenced
  a category with no matching `Category` document (21 categories present instead of
  the canonical 22). Confirmed via `data/seedData.ts` and site-wide usage (Navbar,
  planning wizard, category pages) that `bridal-jewellery` is a real, permanent
  category — not a vendor data error. **Fixed:** created the missing `Category`
  document in Mongo using the exact canonical definition from `data/seedData.ts`.
- **3 test enquiry documents** — all traced to the same source (phone `7980406976`,
  site owner's own email, vendor names literally `"aman"` / `"aps camera"`): 2 had
  broken `vendorId` references (vendor IDs that were never real or long since
  removed), 1 was missing the required `city` field. Confirmed as the site owner's
  own test submissions from testing the enquiry flow, not real customer leads.
  **Fixed:** deleted all 3 from the live `enquiries` collection.

## Final run — clean

```
Migration Readiness Report
===========================
Vendors:             93
Categories:          22
Blogs:                47
Bookings:             8
Enquiries:            1
Consultations:        16
Invoices:             1
Leads:                0
Vendor Applications:  2

Blocking issues: 0
Warnings:        2

⚠️  WARNINGS (non-blocking, review recommended):
   [Booking item references a since-removed vendor] Booking 6a12882fa45abbd29d60be56 item "Touch Of Cozy" has vendorId "venue-1779597985288" with no matching Vendor.id
   [Booking item references a since-removed vendor] Booking 6a12c8f17a9d1c2b58f4e569 item "Touch Of Cozy" has vendorId "venue-1779597985288" with no matching Vendor.id

READY FOR MIGRATION ✅
```

The 2 remaining warnings are legitimate historical data, not errors: "Touch Of Cozy"
was a real, bookable vendor at the time these 2 bookings were placed, and was later
removed from the DB-driven vendor list in favor of its own static local-SEO landing
page (`app/lp/touch-of-cozy-patna`). `BookingItem.vendorId` is nullable by design in
`prisma/schema.prisma` specifically to tolerate this kind of denormalized historical
snapshot — no data-integrity action needed.

## Re-running this check

```
node --env-file=.env.local scripts/validate-migration-readiness.mjs
```

Exits 0 (ready) if there are no blocking issues, 1 otherwise. Safe to re-run any
time — read-only, makes no writes. Recommended before every rehearsal/production
migration run described in `docs/postgres-migration-plan.md` §3 Decision 3.
